# Admin login (single account). Set these in .env for local use.
# Note: These values are visible in the client bundle; use only for a simple single-owner setup.
VITE_ADMIN_EMAIL=
VITE_ADMIN_PASSWORD=
